package LibrarySystem.Users;

public class Librarian extends  User {
    public Librarian(int librarianId,
                     String name,
                     String email,
                     String password) {
        super(librarianId, name, email, password);
    }
}
